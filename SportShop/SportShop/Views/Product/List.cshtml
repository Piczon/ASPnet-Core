@model IEnumerable<Product>

@foreach(var p in Model)
{
    <div class="box" style="border: 0.2px solid #000; padding:10px; ">
        <h3>@p.Name</h3>
            @p.Description
        <h4>Cena: @p.Price.ToString("c")</h4>
        <h6>Kategoria: @p.Category</h6>
    </div>
}

